# `bamd` - block architected markdown documents

Typescript-first Markdown designed for complex prompts

## What is `bamd`?

`bamd` allows you to write expressive, complex Markdown documents that is context-agnostic.

```ts

```
